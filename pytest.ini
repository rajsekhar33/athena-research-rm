[pytest]
testpaths = tests
markers =
    data: requires real simulation data (auto-skipped if ATHENA_RESEARCH_TEST_DATA / the default sample path is unavailable)
    gpu: requires a working CUDA/CuPy GPU (auto-skipped otherwise)
